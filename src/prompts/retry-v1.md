Your previous output was invalid. The system could not parse it.

You MUST respond with ONLY valid JSON. No markdown, no explanation, no code blocks.

Requirements:
- Output starts with { or [
- Output ends with } or ]
- No text before or after the JSON
- All strings properly escaped
- All required fields present

Try again with the exact same request, producing valid JSON only.
